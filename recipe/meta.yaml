{% set name = "papermill" %}
{% set version = "2.1.2" %}
{% set sha256 = "aadc23f0ae2eaa75868e4359f1ea7d75ff46bc8cb1988651f3f3fd5d4ec44679" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  entry_points:
    - papermill = papermill.cli:papermill
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - ansiwrap
    - backports.tempfile
    - click
    - entrypoints
    - future
    - ipython >=5.0
    - jupyter_client
    - nbconvert >=5.5
    - nbformat
    - pandas
    - pyyaml
    - requests
    - six
    - tenacity
    - tqdm >=4.29.1

test:
  imports:
    - papermill
  commands:
    - papermill --help

about:
  home: http://github.com/nteract/papermill
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Papermill parameterizes, executes, and analyzes Jupyter Notebooks
  doc_url: http://papermill.readthedocs.io
  dev_url: https://github.com/nteract/papermill
